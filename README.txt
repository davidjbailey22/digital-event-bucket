Overview

Small desktop app that allows user to randomly select a value in a sqlite database

Example

Enter type in Python GUI and based on event selected, a value will be shown in desktop popup. Record will also be deactivated (complete = N) upon closing of app
